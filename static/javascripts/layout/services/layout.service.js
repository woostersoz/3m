/**
* Layout
* @namespace mmm.layout.services
*/
(function () {
  'use strict';

  angular
    .module('mmm.layout.services')
    .factory('socket', socket);
  
  function socket() {
/*	  var socket = io.connect(
		        ":8000/notifications", // http://app.claritix.io/notifications
		        {
		            "reconnectionDelay": 5000,
		            "timeout": 10000,
		            "resource": "socket.io",
		           
		        }
		    );
       return socket;
		    */
	   
  }

})();